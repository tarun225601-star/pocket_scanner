name: pocket_scanner
description: A pocket OBD2 scanner app for Android and iOS.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.31.6
  permission_handler: ^11.3.1
  url_launcher: ^6.2.5

flutter:
  uses-material-design: true
